import numpy as np
import matplotlib as mpl
import matplotlib as plt
import seaborn as sns


# np.random.seed(map(ord, "aesthetics"))

def sinplot(flip=1):
    x = np.linespace(0, 14, 100)
    plt.plot(x, [1, 2, 3])
    # for i in range(1, 7):


sinplot()
